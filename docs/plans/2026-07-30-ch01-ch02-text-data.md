# ch01 + ch02 实现计划：引言与处理文本数据

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成《从零构建大模型》第 1 章（引言笔记）与第 2 章（文本数据处理）的全部产出：将任意原始文本转化为可供 GPT 训练的 (input, target) token 张量批次。

**Architecture:** 第 2 章是整个项目的数据基石，实现一条清晰的流水线：原始文本 → BPE 分词（tiktoken）→ 词表整数 ID → 滑动窗口生成 (input, target) 对 → `Dataset` → `DataLoader` 批次。所有代码设备无关，统一用 PyTorch。流水线的成熟部分收敛到 `src/gpt/` 供后续章节复用。

**Tech Stack:** Python 3.14（系统环境）、PyTorch 2.13.0+cu130（CUDA）、tiktoken（OpenAI BPE 分词器）、Jupyter。

---

## 前置：环境就绪（✅ 已完成）

> 计划原设想用 Python 3.12 venv + cu121 torch，但执行中发现 2.4GB CUDA 包下载不稳定。
> 后经核查，**系统 Python 3.14 已装好 torch 2.13.0+cu130，CUDA 可用**，遂放弃 venv 直接用系统环境。

- [x] **Step 0.1：核查系统环境**

```bash
python -c "import torch; print('torch:', torch.__version__, '| CUDA:', torch.cuda.is_available(), '|', torch.cuda.get_device_name(0))"
```
实际输出：`torch: 2.13.0+cu130 | CUDA: True | NVIDIA GeForce RTX 5070 Ti Laptop GPU`
→ 系统 Python 3.14 环境完备，无需另装。

- [x] **Step 0.2：补装 jupyter（系统缺这个）**

```bash
python -m pip install jupyter ipykernel
```

- [x] **Step 0.3：把 tiktoken 加入 requirements.txt** ✓

> ~~原 Step 0.1-0.4（装 3.12 / 建 venv / 装 cu121 torch）~~：经实测系统 3.14 已满足，跳过。

---

## 文件结构

本计划涉及创建/修改的文件：

```
ch01-introduction/
└── notes.md                          ← 填充引言笔记（已存在模板）

ch02-text-data/
├── notes.md                          ← 填充第 2 章笔记（已存在模板）
├── ch02.ipynb                        ← 主 notebook：完整流水线演示（含中文注释）
└── solution.py                       ← 整理版代码：GPTDatasetV1 + create_dataloader_v1

src/gpt/
└── data.py                           ← 复用模块：数据流水线（从 solution.py 提取）
```

**职责边界：**
- `ch02.ipynb`：教学演示，逐 cell 讲解，适合交互式学习。
- `solution.py`：本章代码的整理版，可独立运行一个 demo。
- `src/gpt/data.py`：第 2 章成熟组件的归宿，供 ch03+ 复用。接口与 `solution.py` 一致。

---

## Task 1：第 1 章 — 引言笔记（无代码）

**Files:**
- Modify: `ch01-introduction/notes.md`

第 1 章原书无代码，产出是建立全局认知的中文笔记。

- [ ] **Step 1.1：填充 ch01 笔记 — 本章目标与 LLM 全景**

Modify: `ch01-introduction/notes.md`，在「本章目标」下写：

```markdown
本章不写代码。目标是从全局理解：什么是 LLM、它如何从 Transformer 演化而来、
现代 LLM 的两阶段范式（预训练 + 微调）、以及本书"从零搭建 GPT"的学习路线。
```

在「核心内容」下补充 LLM 发展脉络：从 RNN → Transformer(2017) → GPT 系列 → 指令微调/RLHF。

- [ ] **Step 1.2：填充 ch01 笔记 — 关键概念中英对照表**

在「关键概念」表格中填入：

| 中文 | English | 说明 |
|------|---------|------|
| 大语言模型 | LLM (Large Language Model) | 基于海量文本训练的大型神经网络语言模型 |
| Transformer | Transformer | 2017 年提出的注意力架构，现代 LLM 的基石 |
| 自回归 | Autoregressive | 逐 token 预测下一个 token 的生成方式 |
| 预训练 | Pretraining | 在大规模无标注文本上做下一词预测训练 |
| 微调 | Fine-tuning | 在预训练模型上用有标注数据适配下游任务 |
| 指令微调 | Instruction fine-tuning | 用"指令-回答"对让模型学会遵循指令 |
| 词表 | Vocabulary | 模型能识别的所有 token（基本单元）集合 |
| 词元 | Token | 文本被切分后的最小单位 |
| 上下文长度 | Context size | 模型一次能处理的最大 token 数 |

- [ ] **Step 1.3：填充 ch01 笔记 — 本书的代码路线**

在「代码走读」下写明本项目 7 章 + 5 扩展的依赖关系（直接引用 `docs/roadmap.md`）。

- [ ] **Step 1.4：标记 ch01 状态为已完成并提交**

Modify: `ch01-introduction/notes.md` 顶部状态改为 `[x] 已完成`。

```bash
cd /e/repos/python/llm-from-scratch
git add ch01-introduction/notes.md
git commit -m "第 1 章：引言笔记（LLM 全景、两阶段范式、学习路线）"
```

---

## Task 2：第 2 章 — 准备数据文件

**Files:**
- Create: `data/the-verdict.txt`（被 gitignore，不入库）

第 2 章用原书的短篇 "The Verdict" 作为预训练 demo 语料。

- [ ] **Step 2.1：下载 The Verdict 文本**

```bash
cd /e/repos/python/llm-from-scratch
curl -L -o data/the-verdict.txt https://raw.githubusercontent.com/rasbt/LLMs-from-scratch/main/ch02/01_main-chapter-code/the-verdict.txt
```

- [ ] **Step 2.2：验证文件**

```bash
python -c "text=open('data/the-verdict.txt', encoding='utf-8').read(); print('字符数:', len(text)); print('前 50 字符:', repr(text[:50]))"
```

Expected: 字符数约 20479，前 50 字符为 `I HAD always thought Jack Gisburn rather a cheap g`

- [ ] **Step 2.3：确认该文件未被 git 跟踪**

```bash
git status --short data/
```

Expected: 无输出（`data/the-verdict.txt` 被 `.gitignore` 排除，符合预期）。

---

## Task 3：第 2 章 — 分词与词表 ID（notebook 前半部分）

**Files:**
- Create: `ch02-text-data/ch02.ipynb`

实现"原始文本 → token 整数 ID"。

- [ ] **Step 3.1：创建 notebook 并写第一个 cell — 读取文本**

`ch02-text-data/ch02.ipynb` 第一个 cell（Markdown 标题）：
```
# 第 2 章：处理文本数据
## 流水线：原始文本 → 分词 → token ID → 滑动窗口 → DataLoader 批次
```

第二个 cell（读取文本）：
```python
# 读取预训练 demo 语料 "The Verdict"
with open("../data/the-verdict.txt", "r", encoding="utf-8") as f:
    raw_text = f.read()

print(f"语料总字符数: {len(raw_text)}")
print(f"前 100 个字符:\n{raw_text[:100]}")
```

- [ ] **Step 3.2：用正则实现简单分词（教学过渡）**

第三个 cell（说明：这是过渡，最终用 BPE）：
```python
import re

# 用正则做一个最朴素的分词：按空白和标点切分
preprocessed = re.split(r'([,.?_!"()\']|--|\s)', raw_text)
preprocessed = [item.strip() for item in preprocessed if item.strip()]
print(f"切分后 token 数: {len(preprocessed)}")
print(f"前 30 个 token: {preprocessed[:30]}")
```

- [ ] **Step 3.3：构建词表与字符串↔整数映射**

第四个 cell：
```python
# 去重 + 排序，得到词表
all_tokens = sorted(set(preprocessed))
all_tokens.extend(["<|endoftext|>", "<|unk|>"])  # 特殊 token：文本结束 / 未知词

vocab_size = len(all_tokens)
print(f"词表大小: {vocab_size}")

# 字符串 → 整数 ID
str_to_int = {token: i for i, token in enumerate(all_tokens)}
# 整数 ID → 字符串
int_to_str = {i: token for i, token in enumerate(all_tokens)}

def simple_tokenizer(text):
    """简单分词器：文本 → token ID 列表"""
    tokens = re.split(r'([,.?_!"()\']|--|\s)', text)
    tokens = [t.strip() for t in tokens if t.strip()]
    return [str_to_int.get(t, str_to_int["<|unk|>"]) for t in tokens]

# 验证
ids = simple_tokenizer("Hello, world!")
print(f"token IDs: {ids}")
```

- [ ] **Step 3.4：引入 BPE 分词器（tiktoken）**

第五个 cell（解释为何用 BPE 而非简单分词）：
```python
# 简单分词的问题：词表会随语料无限膨胀，遇到新词只能映射成 <unk>。
# BPE（Byte Pair Encoding）把未知词拆成已知子词，词表固定，泛化更好。
# 这里直接用 OpenAI 的 BPE 实现 tiktoken（GPT-2/GPT-3 同款分词器）。

import tiktoken

# 加载 GPT-2 的 BPE 分词器（词表大小 50257）
tokenizer = tiktoken.get_encoding("gpt2")

text = "Hello, world! Is this-- a test?"
ids = tokenizer.encode(text)         # 文本 → token ID
decoded = tokenizer.decode(ids)      # token ID → 文本

print(f"BPE 编码: {ids}")
print(f"解码还原: {decoded}")
print(f"GPT-2 词表大小: {tokenizer.n_vocab}")
```

- [ ] **Step 3.5：提交**

```bash
git add ch02-text-data/ch02.ipynb
git commit -m "第 2 章：分词与词表 ID（简单分词 → BPE/tiktoken）"
```

---

## Task 4：第 2 章 — 滑动窗口生成 (input, target) 对

**Files:**
- Modify: `ch02-text-data/ch02.ipynb`

- [ ] **Step 4.1：演示滑动窗口概念**

新增 cell：
```python
# LLM 是自回归模型：根据前 N 个 token 预测第 N+1 个 token。
# 用滑动窗口把长 token 序列切成无数 (input, target) 训练对：
#   input  = token[i : i+max_len]
#   target = token[i+1 : i+max_len+1]   ← target 比 input 右移一位

import torch

# 先把整篇语料编码成 token ID
with open("../data/the-verdict.txt", "r", encoding="utf-8") as f:
    raw_text = f.read()

enc_text = tokenizer.encode(raw_text)
print(f"整篇语料 token 数: {len(enc_text)}")
```

- [ ] **Step 4.2：实现并验证滑动窗口**

新增 cell：
```python
context_size = 4         # 上下文长度（demo 用小值，真实 GPT 是 256/1024）
stride = 1               # 滑动步长

# 切出前几对 (input, target) 演示
for i in range(0, 8):    # 只演示前 8 对
    input_chunk = enc_text[i : i + context_size]
    target_chunk = enc_text[i + 1 : i + context_size + 1]
    print(f"--- 第 {i} 对 ---")
    print(f"input : {input_chunk}  → {tokenizer.decode(input_chunk)}")
    print(f"target: {target_chunk}  → {tokenizer.decode(target_chunk)}")
```

验证：每对的 target 应是 input 右移一位。如第 0 对 input=`[40, 367, 2885, 1464]`，则第 0 对 target=`[367, 2885, 1464, 1807]`。

- [ ] **Step 4.3：提交**

```bash
git add ch02-text-data/ch02.ipynb
git commit -m "第 2 章：滑动窗口生成 (input, target) 训练对"
```

---

## Task 5：第 2 章 — GPTDatasetV1 与 DataLoader（核心产出）

**Files:**
- Create: `src/gpt/data.py`
- Create: `ch02-text-data/solution.py`

这是第 2 章的标志性产出，也是后续章节复用的核心模块。

- [ ] **Step 5.1：在 src/gpt/data.py 写 GPTDatasetV1**

Create: `src/gpt/data.py`

```python
"""第 2 章核心数据模块：把原始文本转化为 GPT 训练用的 token 张量批次。

流水线：原始文本 → BPE 编码 → 滑动窗口生成 (input, target) 对 → PyTorch Dataset
"""

import torch
from torch.utils.data import Dataset, DataLoader


class GPTDatasetV1(Dataset):
    """自回归语言模型的训练数据集。

    对一段已编码的 token 序列施加滑动窗口，每个样本是一对
    (input_chunk, target_chunk)，target 比 input 右移一位。
    """

    def __init__(self, txt: str, tokenizer, max_length: int, stride: int):
        """初始化数据集。

        Args:
            txt: 原始文本。
            tokenizer: 带 encode 方法的分词器（如 tiktoken）。
            max_length: 每个样本的 token 数（上下文长度）。
            stride: 滑动步长。通常等于 max_length 以避免样本重叠；
                    设为 1 可最大化样本数但会高度重叠。
        """
        self.input_ids = []
        self.target_ids = []

        # 整篇文本一次性编码成 token ID
        token_ids = tokenizer.encode(txt)

        # 滑动窗口切分（确保最后一段不满 max_length 的被丢弃）
        for i in range(0, len(token_ids) - max_length, stride):
            input_chunk = token_ids[i : i + max_length]
            target_chunk = token_ids[i + 1 : i + max_length + 1]
            self.input_ids.append(torch.tensor(input_chunk))
            self.target_ids.append(torch.tensor(target_chunk))

    def __len__(self) -> int:
        return len(self.input_ids)

    def __getitem__(self, idx: int):
        return self.input_ids[idx], self.target_ids[idx]
```

- [ ] **Step 5.2：在 src/gpt/data.py 写 create_dataloader_v1**

追加到 `src/gpt/data.py`：

```python


def create_dataloader_v1(
    txt: str,
    batch_size: int = 4,
    max_length: int = 256,
    stride: int = 128,
    shuffle: bool = True,
    drop_last: bool = True,
    num_workers: int = 0,
):
    """从原始文本创建一个 Yield (input_batch, target_batch) 的 DataLoader。

    Args:
        txt: 原始文本。
        batch_size: 每个批次的样本数。
        max_length: 每个样本的 token 数。
        stride: 滑动步长。
        shuffle: 是否打乱。
        drop_last: 是否丢弃最后不满一个 batch 的样本。
        num_workers: DataLoader 工作进程数。

    Returns:
        配置好的 torch.utils.data.DataLoader。
    """
    # 延迟导入，避免在没装 tiktoken 的环境里 import 本模块就报错
    import tiktoken

    tokenizer = tiktoken.get_encoding("gpt2")
    dataset = GPTDatasetV1(txt, tokenizer, max_length, stride)

    dataloader = DataLoader(
        dataset,
        batch_size=batch_size,
        shuffle=shuffle,
        drop_last=drop_last,
        num_workers=num_workers,
    )
    return dataloader
```

- [ ] **Step 5.3：更新 src/gpt/__init__.py 导出**

Modify: `src/gpt/__init__.py`，替换 `__all__` 那行为：

```python
from src.gpt.data import GPTDatasetV1, create_dataloader_v1

__all__ = ["GPTDatasetV1", "create_dataloader_v1"]
```

- [ ] **Step 5.4：写验证脚本测试 DataLoader 形状**

创建临时验证（直接命令行，不落库）：

```bash
cd /e/repos/python/llm-from-scratch
python -c "
import tiktoken
from src.gpt.data import create_dataloader_v1

with open('data/the-verdict.txt', encoding='utf-8') as f:
    txt = f.read()

dataloader = create_dataloader_v1(txt, batch_size=8, max_length=4, stride=4, shuffle=False)
data_iter = iter(dataloader)
inputs, targets = next(data_iter)
print('inputs 形状:', inputs.shape)    # 期望 torch.Size([8, 4])
print('targets 形状:', targets.shape)  # 期望 torch.Size([8, 4])
print('第一个 batch 的 inputs:\n', inputs)
print('第一个 batch 的 targets:\n', targets)
# 关键断言：target 应是 input 右移一位（在同一行内）
assert torch.equal(inputs[:, 1:], targets[:, :-1]), 'target 应为 input 右移一位'
print('✅ 验证通过')
"
```

Expected: `inputs 形状: torch.Size([8, 4])`、`targets 形状: torch.Size([8, 4])`、`✅ 验证通过`

- [ ] **Step 5.5：写 ch02-text-data/solution.py（整理版 demo）**

Create: `ch02-text-data/solution.py`

```python
"""第 2 章整理版代码：端到端演示「文本 → 训练批次」。

运行：python ch02-text-data/solution.py
"""

import torch

# 让本脚本能从仓库根目录导入 src 包
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from src.gpt.data import create_dataloader_v1  # noqa: E402


def main():
    # 1. 读取语料
    data_path = Path(__file__).resolve().parent.parent / "data" / "the-verdict.txt"
    with open(data_path, "r", encoding="utf-8") as f:
        raw_text = f.read()
    print(f"[1/3] 已读取语料，共 {len(raw_text)} 字符")

    # 2. 构建 DataLoader
    dataloader = create_dataloader_v1(
        raw_text, batch_size=8, max_length=4, stride=4, shuffle=False
    )
    print(f"[2/3] 数据集共 {len(dataloader.dataset)} 个样本，{len(dataloader)} 个批次")

    # 3. 取出第一个批次展示
    inputs, targets = next(iter(dataloader))
    print(f"[3/3] 第一个批次:")
    print(f"  inputs  形状: {inputs.shape}, dtype: {inputs.dtype}")
    print(f"  targets 形状: {targets.shape}, dtype: {targets.dtype}")
    print(f"  inputs[0]:  {inputs[0].tolist()}")
    print(f"  targets[0]: {targets[0].tolist()}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 5.6：运行 solution.py 确认无报错**

```bash
cd /e/repos/python/llm-from-scratch
python ch02-text-data/solution.py
```

Expected: 打印字符数、样本数、批次形状，无异常。

- [ ] **Step 5.7：提交**

```bash
git add src/gpt/data.py src/gpt/__init__.py ch02-text-data/solution.py
git commit -m "第 2 章：GPTDatasetV1 + create_dataloader_v1（文本→训练批次，src/gpt 复用模块）"
```

---

## Task 6：第 2 章 — 补全 notebook 末尾与笔记

**Files:**
- Modify: `ch02-text-data/ch02.ipynb`
- Modify: `ch02-text-data/notes.md`

- [ ] **Step 6.1：在 notebook 末尾用 src/gpt 模块收尾**

新增 cell（呼应 src/gpt/data.py，演示复用）：
```python
# 本章核心组件已提取到 src/gpt/data.py，供后续所有章节复用。
import sys; sys.path.insert(0, "..")
from src.gpt.data import create_dataloader_v1

dataloader = create_dataloader_v1(raw_text, batch_size=8, max_length=4, stride=4, shuffle=False)

# 注意：在真实训练中，max_length 通常设为 256，stride 等于 max_length，
# batch_size 设为 GPU 能容纳的大小。这里用小值仅为演示。
for batch_idx, (x, y) in enumerate(dataloader):
    if batch_idx >= 2:
        break
    print(f"批次 {batch_idx}: x={x.shape}, y={y.shape}")
```

- [ ] **Step 6.2：补全 ch02 笔记 — 概念表**

Modify: `ch02-text-data/notes.md`，在「关键概念」表填入：

| 中文 | English | 说明 |
|------|---------|------|
| 分词 | Tokenization | 把文本切成 token 的过程 |
| 字节对编码 | BPE (Byte Pair Encoding) | 把未知词拆成已知子词的分词算法 |
| 词表 | Vocabulary | 所有 token 的 ID 映射 |
| 上下文长度 | Context length / max_length | 一次输入的 token 数 |
| 滑动窗口 | Sliding window | 在长序列上滑动生成训练样本 |
| 步长 | Stride | 每次滑动的距离 |
| 嵌入 | Embedding | 把 token ID 映射为稠密向量（第 2 章末引入，第 3 章详讲） |

- [ ] **Step 6.3：补全 ch02 笔记 — 代码走读与踩坑**

在「代码走读」写明三个核心件的职责：`tokenizer.encode`、`GPTDatasetV1`、`create_dataloader_v1`。
在「踩坑记录」预留一条占位：Windows 下相对路径需注意 cwd（notebook 从章节目录运行用 `../data`，脚本从根目录运行用 `data/`）。

- [ ] **Step 6.4：标记 ch02 状态为已完成并提交**

Modify: `ch02-text-data/notes.md` 顶部状态改为 `[x] 已完成`。

```bash
git add ch02-text-data/ch02.ipynb ch02-text-data/notes.md
git commit -m "第 2 章：补全 notebook 收尾与中文笔记"
```

---

## 验收标准（Definition of Done）

执行完所有任务后，以下必须全部成立：

- [ ] `python ch02-text-data/solution.py` 能跑通，打印批次形状 `[8, 4]` 无报错
- [ ] notebook 从头到尾顺序执行无报错（Kernel → Restart & Run All）
- [ ] `from src.gpt.data import create_dataloader_v1` 可在任意章节目录导入
- [ ] 断言 `target == input 右移一位` 成立
- [ ] `data/the-verdict.txt` 存在但未被 git 跟踪（被 gitignore）
- [ ] ch01、ch02 的 `notes.md` 状态标记为 `[x]`
- [ ] 每个任务都有独立 commit，提交信息为中文

## 自审清单

- ✅ **Spec 覆盖**：spec 第 5 节 ch01（笔记为主）、ch02（分词/滑动窗口/dataloader）均有对应任务
- ✅ **无占位符**：所有代码步骤都给了完整可运行代码，无 TODO
- ✅ **类型/命名一致**：`GPTDatasetV1`、`create_dataloader_v1`、`max_length`、`stride` 在 Task 5-6 全程一致
- ✅ **路径一致**：notebook 用 `../data`（从章节目录运行），solution.py 用绝对定位，src 模块用 `sys.path` 注入，三处都覆盖
