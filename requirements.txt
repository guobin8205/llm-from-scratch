# ============================================================
# 《从零构建大模型》课程代码依赖
# 注意：PyTorch 需安装 CUDA 版本以启用 GPU 加速
# 安装命令见 README.md「环境配置」一节
# ============================================================

# ---- 核心框架（CUDA 版 PyTorch，见下方说明）----
# torch>=2.2            # 不要直接 pip install，用官方 CUDA 索引安装
# torchvision>=0.17     # 同上

# ---- 数值与可视化 ----
numpy>=1.26
matplotlib>=3.8
tqdm>=4.66

# ---- 分词器（ch02 起）----
tiktoken>=0.7

# ---- Jupyter 交互式学习 ----
jupyter>=1.0
ipykernel>=6.29

# ===== 各章 bonus 依赖（按需安装）=====

# UI 类 bonus（ch05/06/07 的 *-user-interface.ipynb）
gradio>=4.0
# ch06 bonus 02-more-datasets（IMDb csv 读取，有 try/except 回退）
pandas>=2.0

# ===== 扩展章节专用依赖（按需安装）=====

# ext-quantization / appendix-E-lora (QLoRA)：模型量化与低秩微调
# bitsandbytes>=0.43          # int8/int4 量化

# ext-rag：工程化 RAG
# langchain>=0.1
# langchain-community>=0.0
# chromadb>=0.4               # 向量数据库
# sentence-transformers>=2.5  # embedding 模型

# ext-lora：更完整的 PEFT 工具链（可选）
# peft>=0.10

# PyTorch CUDA 安装说明（系统 Python 3.14，RTX 5070 Ti，已装 2.13.0+cu130）：
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
# （实际本项目用系统已装好的 torch，无需重复安装）
